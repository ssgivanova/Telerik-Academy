function solve(args) {
    //var n = +args[0], // cast string to Number
    var n = parseInt(args[0]);
        
    if(n%2===0){
            console.log("even "+n);
        
    }else{
        console.log("odd "+n);
    }
}